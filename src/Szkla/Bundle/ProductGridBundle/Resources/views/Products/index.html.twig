{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set pageTitle = 'Products'|trans %}

{% block navButtons %}
    {% if resource_granted('szkla_products.products_view') %}
        <div class="btn-group">
            {{ UI.addButton({
                'path': path('szkla_products.product_create'),
                'entity_label': 'Product'|trans
            }) }}
        </div>
    {% endif %}
{% endblock %}